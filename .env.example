# Database connection URL (PostgreSQL)
# For NeonDB, ensure sslmode=require is included
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Secret key for JWT token signing
# Generate a strong secret key for production
JWT_SECRET=your-super-secret-jwt-key

# Port for the server (optional, defaults to 3000)
PORT=3000

# Node environment (development or production)
NODE_ENV=development

# Firebase Configuration
FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com/
# For production, use service account (as single-line JSON)
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id",...}
# For development only (legacy database secret)
FIREBASE_DATABASE_SECRET=your-database-secret

# For frontend Firebase client
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com/